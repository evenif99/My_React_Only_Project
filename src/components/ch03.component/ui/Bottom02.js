function App({ goodbye, comment }) {
    // console.log('샘플');

    return (
        <header>
            <h1>{goodbye}</h1>
            <p>{comment}</p>
        </header>
    );
}

export default App;